---
description: Fetch all unresolved review comments on a GitHub PR and address them in a single pass (Mode B review workflow). Usage: /address-pr <PR number>
---

Use the `pr-review-address` skill to address all unresolved review comments on PR #$ARGUMENTS in a single pass. Fetch threads, make all changes, reply and resolve each addressed thread, commit, and push.
